# mock | live
LLM_MODE=mock

# Only needed for live mode (run.py) and CrewAI live demo (crew_resolver.py)
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o-mini

# Optional (advanced): set if your org uses a compatible gateway/proxy
# OPENAI_BASE_URL=
